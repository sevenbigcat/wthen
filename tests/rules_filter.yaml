rules:
  - rule:
      name: f1
      pre:
        - aa = 4
      when:
        - aa > bb
      then:
        - print(aa, '>', bb)
  - rule:
      name: f2
      when:
        - cc < dd
      then:
        - print(cc, '>', dd)
